# คลัง IRiSH LAB — คู่มือระบบฉบับเต็ม

เอกสารนี้อธิบายระบบทั้งหมด: ทำไมถึงสร้าง, ทำงานยังไง, มีอะไรบ้าง, และดูแลรักษายังไง
สำหรับคู่มือสั้นเชิงเทคนิค (ให้ Claude/นักพัฒนา) ดูที่ [CLAUDE.md](CLAUDE.md)

---

## 1. เป้าหมาย — ทำโปรแกรมนี้ขึ้นมาทำไม

**ปัญหาเดิม:** ห้องแลป IRiSH (หุ่นยนต์/อิเล็กทรอนิกส์) มีของเยอะ — เครื่องมือ, บอร์ด, เซนเซอร์, แบต, สายไฟ, ชิ้นส่วนหุ่น — และมีคนหมุนเวียนยืมของ (นักศึกษา, อาจารย์, คนนอก) การจดในสมุด/Excel ทำให้:
- ไม่รู้ว่าของชิ้นไหนอยู่ที่ใคร ใครยืมไปนานแล้วยังไม่คืน
- ของหายแล้วไม่รู้ว่าหายตั้งแต่เมื่อไหร่ ใครถือล่าสุด
- นับสต็อกไม่ตรง เพราะของบางส่วนถูกเอาไปประกอบเป็นหุ่นแล้ว แต่ในกระดาษยังนับว่าอยู่ในคลัง
- ของใช้แล้วทิ้ง (สายไฟ, วัสดุสิ้นเปลือง) เบิกไปเรื่อยๆ ไม่มีใครคุมยอด

**เป้าหมายของระบบ:**
1. **รู้ตำแหน่งของทุกชิ้นตลอดเวลา** — อยู่ในคลัง / ถูกยืม (ใครยืม) / อยู่ในหุ่นตัวไหน / พัง / หาย
2. **ยืม-คืนง่าย** พอที่นักศึกษาและคนนอกจะใช้เองได้ ไม่ต้องผ่านขั้นตอนยุ่งยาก
3. **นับสต็อกให้ตรงความจริง** โดยเฉพาะของที่ถูกกันไว้ประกอบหุ่น (กันไว้ แต่ยังไม่ถูกยืมจริง)
4. **มีประวัติครบ** ทุกการเคลื่อนไหวย้อนดูได้ ใครทำอะไรเมื่อไหร่
5. **ใช้ได้จากมือถือ** เดินถือมือถือในห้องแลปแล้วเช็ค/ยืม/คืนได้เลย
6. **เปิดสาธารณะ** ให้คนนอก (เช่นอาจารย์ที่ขี้เกียจล็อกอิน) ดูของและยืมได้ โดยไม่ต้องสร้างบัญชี

**สเกล:** ห้องแลปเล็ก (<10 คนหลัก) + คนนอกที่แวะมายืมเป็นครั้งคราว

---

## 2. ภาพรวมสถาปัตยกรรม

```
┌─────────────────────────────────────────────────────┐
│  เบราว์เซอร์ (คอม/มือถือ)                              │
│  React SPA (Vite build → public/)                    │
└───────────────────────┬─────────────────────────────┘
                        │ HTTP /api/*  (cookie-session)
┌───────────────────────▼─────────────────────────────┐
│  Express server (server.js)                          │
│  - เสิร์ฟ public/ (หน้าเว็บที่ build แล้ว)             │
│  - REST API + สิทธิ์ (requireAuth / requireAdmin)     │
└───────────────────────┬─────────────────────────────┘
                        │ libsql sync API (better-sqlite3-compatible)
┌───────────────────────▼─────────────────────────────┐
│  DB (db.js)                                          │
│  - โลคัล: SQLite ไฟล์เดียว data/inventory.db (WAL)    │
│  - โปรดักชัน: Turso embedded replica (sync ขึ้นคลาวด์)│
└─────────────────────────────────────────────────────┘
```

### Stack
- **Backend:** Node.js + Express + libsql (API เข้ากันได้กับ better-sqlite3 — เรียกแบบ sync)
- **Frontend:** React 18 + Vite → build ออกไปที่ `public/` ให้ Express เสิร์ฟ (SPA ไฟล์เดียว)
- **Database:** SQLite ไฟล์เดียว โหมด WAL; โปรดักชันใช้ **Turso embedded replica** (อ่าน/เขียนโลคัลเร็ว แล้ว sync ขึ้นคลาวด์เป็นระยะ)
- **Deploy:** Render (auto-deploy จาก git push, `render.yaml` เป็นตัวกำหนด build/start)

### กฎเหล็กตอนพัฒนา
> แก้ไฟล์ใน `client/src/` แล้ว **ต้อง `cd client && npm run build` เสมอ** ก่อน commit
> เพราะ Express เสิร์ฟจาก `public/` (ผลลัพธ์ที่ build แล้ว) — ถ้าไม่ build จะไม่เห็นผลบน :3000 และของที่ push ไปจะไม่อัปเดต

### วิธีรัน
```bash
npm start                      # backend + เว็บจริง (พอร์ต 3000)
cd client && npm run dev       # โหมดพัฒนา UI (พอร์ต 5173, proxy /api ไป 3000)
cd client && npm run build     # build UI → public/  (ต้องทำก่อน push ทุกครั้งที่แก้ client)
```

### บัญชีทดสอบ
- admin: `admin` / `admin1234`
- staff: `staff1` / `staff123`

---

## 3. บทบาทผู้ใช้ (Roles) และสิทธิ์

| บทบาท | ยืม/เบิก | อนุมัติ/รับคืน | เพิ่ม/แก้ของ · จัดการหน่วย | ดูประวัติ · จัดการผู้ใช้ | ต้องล็อกอิน |
|-------|:------:|:------:|:------:|:------:|:------:|
| **admin** | ✅ | ✅ | ✅ | ✅ | ✅ |
| **staff** | ✅ (ขอ) | ❌ | ❌ | ❌ | ✅ |
| **guest** (คนนอก) | ✅ (ขอ) | ❌ | ❌ | ❌ (เห็นแดชบอร์ด/ของพัง-หายได้) | ❌ ไม่ต้อง |

- **สิทธิ์บังคับที่ server เสมอ** (ไม่ใช่แค่ซ่อนปุ่มใน UI) — ทุก endpoint จัดการผ่าน `requireAuth` / `requireAdmin`
- **guest = คนนอกที่ไม่มีบัญชี** เข้าเว็บสาธารณะแล้วขอยืมได้เลย ระบุตัวตนด้วย **รหัสบัตร** (ดูข้อ 7)

---

## 4. โมเดลข้อมูล (Domain Model)

### ตารางหลัก

| ตาราง | คืออะไร |
|-------|---------|
| **users** | บัญชีผู้ใช้ (admin/staff) + role |
| **items** | "ชนิดของ" 1 แถว = 1 ชนิด (เช่น "Lidar X4 Pro") มี `type`, `category`, `tracked`, `is_kit`, `qty`, รูป |
| **units** | หน่วยย่อยรายตัว (เช่น LIDAR-01, LIDAR-02) สำหรับของที่ `tracked=1` — มี `code` + `status` + `holder` |
| **kit_components** | ชิ้นส่วนที่ประกอบเป็นหุ่น (kit) — ผูก item/unit เข้ากับหุ่น |
| **transactions** | ประวัติทุกการเคลื่อนไหว (add/issue/borrow/return/repair/ready/lost/reserve/release) |
| **requests** | คำขอยืม/เบิก (workflow) |
| **request_units** | หน่วยที่ถูกจองให้คำขอนั้น (คำขอ 1 ใบ อาจผูกหลายหน่วย) |
| **orders** | ตะกร้า = คำขอหลายรายการใน 1 ใบ |
| **locations** | ตู้/ที่เก็บของ |

### ประเภทของ (`type`)
- **tool** = ของที่ยืมแล้วคืน (เครื่องมือ, บอร์ด, เซนเซอร์...) → borrow/return
- **consumable** = ของใช้แล้วหมด (สายไฟตัด, วัสดุสิ้นเปลือง) → issue (เบิกจบ ไม่ต้องคืน)

### หมวดหมู่ (`category`)
รายชื่อหมวดจริงอยู่ที่ `CATEGORY_GROUPS` ใน [client/src/api.js](client/src/api.js) — **นี่คือแหล่งความจริง (source of truth)** ของหมวด
> ⚠️ หมายเหตุการดูแล: การจับคู่ "หมวด → type" มีอยู่ทั้งฝั่ง client (`api.js`) และ server (`CATEGORY_TYPE` ใน `server.js`) — **เพิ่มหมวดใหม่ต้องแก้ทั้งสองที่** ให้ตรงกัน ไม่งั้นเลือกหมวดใหม่แล้วระบบจะเดา type ผิด

- **หมวดใช้แล้วคืน (tool):** เครื่องมือ, ชิ้นส่วน/อุปกรณ์, บอร์ด, แบตเตอรี่, หน่วยความจำ/การ์ด, หุ่นยนต์, สาย USB (ไม่ตัด)
- **หมวดใช้แล้วทิ้ง (consumable):** สายไฟ, วัสดุสิ้นเปลือง, สาย USB (ใช้ตัด)

### track รายตัว (`tracked`)
- **tracked=1** — แยกหน่วยรายตัวมีรหัส (LIDAR-01, LIDAR-02...) รู้ว่าตัวไหนอยู่ที่ใคร เหมาะกับของมีค่า/ต้องระบุตัว
- **tracked=0** — นับเป็นจำนวนรวม (qty) เหมาะกับของเยอะๆ เหมือนกันหมด (น็อต, สายไฟ)

### สถานะหน่วย (units.status)
| สถานะ | ความหมาย |
|-------|----------|
| `available` | ว่างในคลัง ยืมได้ |
| `borrowed` | ถูกยืมออกไป (มี `holder` = ชื่อคนยืม) |
| `reserved` | **กันไว้ประกอบหุ่น** (อยู่ในหุ่น) — ยังอยู่ในคลังจริง แค่กันไว้ |
| `repair` | พัง รอซ่อม (ตัดออกจากคลัง ไปอยู่หน้า "ของพัง/หาย") |
| `lost` | หาย (ตัดออกจากคลัง) |

---

## 5. โมเดลหุ่นยนต์ (Kit / Robot) — จุดที่ซับซ้อนที่สุด

หุ่นยนต์คือ item ที่ `is_kit=1` (บังคับ `tracked=0` เสมอ) ประกอบจากชิ้นส่วนหลายชิ้น

### หลักการ "กันไว้ ไม่ตัดออกจากคลัง" (reserve-on-create)
- **สร้างหุ่น = จองชิ้นส่วนทันที** — ของที่เอาไปประกอบหุ่นถูกเปลี่ยนเป็น `reserved` (หน่วย track) หรือบันทึกแถว `kit_components` (ของไม่ track)
- ของที่ถูกจอง **ยังนับว่าอยู่ในคลัง** (ไม่หายไปจากยอดรวม) แต่ **ไม่นับเป็น "ใช้ได้จริง"** (free) เพราะถูกกันไว้แล้ว
- หุ่นเองแสดงเป็นของ **1 ชิ้น** (ไม่ใช่ "ประกอบได้กี่ตัว")

### ยืมหุ่น = ยืมของทั้งชุด
- ยืมหุ่น 1 ตัว → ชิ้นส่วนข้างในถือว่าออกไปพร้อมหุ่น (ยังเป็น `reserved` แต่ระบบแยกได้ว่า "อยู่ในหุ่นที่ถูกยืมออกไปแล้ว")
- ระบบแยกด้วยยอดหุ่น: `หุ่น.qty >= 1` = หุ่นอยู่ในคลัง (ชิ้นส่วน = "อยู่ในหุ่น") · `หุ่น.qty = 0` = หุ่นถูกยืมออก (ชิ้นส่วน = "ถูกยืมผ่านหุ่น")

### 5 ช่องนับสต็อก (decorateItem)
ทุก item คำนวณ 5 ค่านี้ให้ยอดตรงเสมอ:
| ค่า | ความหมาย |
|-----|----------|
| `total_qty` | ทั้งหมดที่มี (ทุกสถานะรวมกัน) |
| `out_qty` | ถูกยืมออกไปตรงๆ (ไม่ผ่านหุ่น) |
| `in_kit_qty` | กันไว้ในหุ่นที่ยังอยู่ในคลัง |
| `qty` (stock) | คงเหลือในคลัง (= available) |
| `free_qty` | ใช้ได้จริง (คงเหลือ − ที่กันไว้) = ยืมได้เท่านี้ |

> ⚠️ ของที่กันไว้ในหุ่น **ไม่เคยถูกตัดออกจากคลังจริง** — แค่เปลี่ยนสถานะ ทำให้ยอดรวมไม่เพี้ยน

### ปลดชิ้นส่วนออกจากหุ่น
- **คืนคลังทีละชิ้น** — หน้า Stock Check คอลัมน์ "อยู่ในหุ่น" มีปุ่ม **↩ คืนคลัง** ปลดหน่วยนั้นออกจากหุ่นกลับเป็นของว่าง (หุ่นจะขาดชิ้นนั้นไป, หน่วยไม่หาย)
  - กันไว้: หุ่นที่**ถูกยืมออกไปแล้ว** ปลดชิ้นไม่ได้ (ต้องรับคืนหุ่นก่อน กันยอดเพี้ยน)
- **รื้อทั้งหุ่น** — แก้/ลบหุ่น → ชิ้นส่วนทุกชิ้นถูกปลดกลับคลังอัตโนมัติ (`releaseKitReservation`)

---

## 6. Workflow การยืม-คืน

### 6.1 เส้นทางปกติ (staff/guest ขอ → admin อนุมัติ)
```
staff/guest เลือกของ → ใส่ตะกร้า/ยืมเลย → ส่งคำขอ (สถานะ pending)
        ↓
admin เห็นในหน้าคำขอ → เลือกหน่วยจริง (ถ้า track) → กดอนุมัติ
        ↓  (ตัดสต็อกทันที = จบขั้นเดียว ไม่มีขั้นส่งมอบแยก)
สถานะ received (ถูกยืมอยู่) — ถือว่าอยู่กับผู้ขอแล้ว
        ↓
admin กด "✓ รับของคืนแล้ว" ตอนได้ของคืน → สถานะ returned (ของกลับเข้าคลัง)
```
- คืนได้ 3 แบบ: **รับของคืน** (ปกติ) · **แจ้งพัง** (ของ track — ไปหน้าซ่อม) · **แจ้งหาย** (ของ track — ตัดถาวร)
- ปุ่มสำคัญมี dialog ยืนยันกันพลาด

### 6.2 admin ยืมที่หน้ารายการของ (Stock) → เด้งเข้าหน้าคำขออัตโนมัติ
admin ยืม/คืนของตรงหน้ารายการ (Stock Check / เคลื่อนไหวสต็อก) ได้เลยไม่ต้องผ่านคำขอ
**ระบบสร้าง "คำขอ" สถานะ received ให้อัตโนมัติ** พร้อมชื่อผู้ยืม → เด้งในหน้าคำขอเหมือนคนอื่นขอยืมมา (mirror)
- ใช้ตอนคนนอกมายืมแต่ไม่สะดวกกรอกเอง → admin คีย์ให้ ระบุชื่อเขา แล้วชื่อไปโผล่ในหน้าคำขอว่า "ถูกยืมอยู่"
- **sync สองทาง:** คืนที่หน้าไหนก็ได้ — คืนที่หน้ารายการ คำขอปิดเอง / กดรับคืนที่หน้าคำขอ ของก็กลับเข้าคลัง
- ของยืมหลายหน่วย คืนทีละตัว คำขอยังไม่ปิดจนคืนครบ

### 6.3 ของ consumable (เบิกจบ)
- เบิกแล้วหักจำนวน ไม่ต้องคืน (สถานะ returned/closed เลย)
- เบิกจนเหลือ 0 → **หายจากหน้ารายการของ** (ข้อมูล/ประวัติยังอยู่) — admin ติ๊ก "แสดงของที่เบิกหมด" เพื่อดู/เติมสต็อก

---

## 7. ระบบรหัสบัตร (ยืนยันตัวตน guest)

คนนอก (guest) ที่ยืมของ **ต้องกรอกรหัสบัตร** (เลขบัตรประชาชน/นักศึกษา) — staff/admin ไม่ต้อง (ล็อกอินอยู่แล้ว)

**เหตุผล & หลักความเป็นส่วนตัว:**
- รหัสบัตรเป็น "ความลับ" ที่ใช้ **กันคนอื่นสวมสิทธิ์** ยืม/ยกเลิกแทนเรา
- แต่ **การดูรายชื่อคนยืมยังเห็นได้ปกติ** (ไม่ได้ปิดบังว่าใครยืมอะไร) — บัตรแค่ป้องกันการปลอมตัว ไม่ได้ปิดการมองเห็น
- guest ยืนยันตัวด้วยรหัสบัตร (`session.gcard`) หรือ fallback เป็น `guest_key` ต่อเบราว์เซอร์
- มี throttle กันสแปม (จำกัดจำนวนคำขอต่อ IP)

---

## 8. หน้าจอในระบบ

| หน้า | ใครเห็น | ทำอะไร |
|------|---------|--------|
| **แดชบอร์ด** | ทุกคน | สรุปภาพรวม: การ์ดสถิติ (จำนวนรวม/หุ่น/ถูกยืม/คงเหลือ), ของถูกยืม (ใครถือ), หุ่น + ผู้ยืม |
| **รายการของ** | ทุกคน | ตาราง/การ์ดของทั้งหมด, ค้นหา, กรองหมวด, Stock Check (จัดการหน่วยย่อย), ยืม/ขอยืม, admin เพิ่ม/แก้ |
| **คำขอ** | ทุกคน (เห็นของตัวเอง; admin เห็นหมด) | workflow ยืม-คืน, แท็บ รออนุมัติ/ถูกยืมอยู่/คืนแล้ว |
| **ประวัติ** | admin เท่านั้น | ทุก transaction ย้อนหลัง |
| **ผู้ใช้** | admin เท่านั้น | จัดการบัญชี |
| **ผู้ยืม** | admin | รายชื่อคนยืม + สรุปของที่ถืออยู่ |
| **ของพัง/หาย** | ทุกคน (เปิดให้ guest เห็น) | หน่วยที่ repair/lost, กู้คืนได้ |

### หน้ารายการของ (Stock Check) — คอลัมน์
เปิดของที่ track รายตัว จะเห็น 4 คอลัมน์: **ว่าง** (admin เท่านั้น) · **ถูกยืม** · **อยู่ในหุ่น** · **พัง/หาย**
แต่ละหน่วยมีปุ่มจัดการตามสถานะ (ยืม/คืน/แจ้งพัง/แจ้งหาย/คืนคลัง/ลบ)

---

## 9. รูปแบบ UI ที่ใช้ซ้ำ

- **Modal** ผ่าน React Portal (หลุดจาก transform ของ card แม่) + ล็อกสกอลพื้นหลัง + กันปิดพลาดตอนลากเลือกข้อความ (`wide` = กว้าง 620px)
- **DialogProvider** — `useConfirm()` / `usePrompt()` แทน `confirm()`/`prompt()` เนทีฟ, กำหนดสีปุ่มได้ (`okClass`)
- **Toast** — แจ้งเตือนสั้นๆ มุมจอ
- **Optimistic UI** — กดแล้วอัปเดตจอทันที ยิง API เบื้องหลัง แล้ว reconcile (สำเร็จ→refresh, error→refresh+แจ้ง)
- **สีปุ่มการกระทำ:** ยืมเลย=เขียว · ใส่ตะกร้า=ฟ้า · รับของคืน=เขียว · แจ้งพัง=ส้ม · แจ้งหาย/ลบ=แดง · อนุมัติ=ฟ้า
- **สีในตาราง:** ทั้งหมด=น้ำเงิน · ถูกใช้/ยืม=แดง · คงเหลือ=เขียว

---

## 10. รายละเอียดเทคนิคที่ควรรู้

### แนบรูป
ถ่ายจากมือถือได้ ย่ออัตโนมัติเป็น JPEG ~1280px เก็บเป็น **data URL ใน DB** (ไม่เก็บไฟล์บน disk เพราะ host เป็น ephemeral — ล้าง disk ทุก redeploy รูปจะหาย แต่ data URL ใน DB ไป Turso ด้วยเลยรอด)

### Transaction / ความปลอดภัยของยอด
- ทุกการเปลี่ยนยอดอยู่ใน `db.tx()` (BEGIN/COMMIT/ROLLBACK) — พลาดกลางทาง rollback หมด ไม่เหลือข้อมูลค้าง
- ธุรกรรมสำคัญ (อนุมัติ/คืน/ปลดหุ่น) เรียก `db.flushNow()` ดันขึ้น Turso ทันที ไม่รอรอบ sync
- `db.recalcTracked(itemId)` คำนวณ `items.qty` (= จำนวนหน่วย available) ใหม่หลังทุกการเปลี่ยนหน่วย

### Migration
`db.js` มี migration guard ผ่านตาราง `_migrations` — เพิ่มคอลัมน์/ตารางใหม่ทำผ่าน `addColumn` และ migration แบบมีชื่อ รันครั้งเดียว

### ข้อควรระวังตอน dev
- ทดสอบ API ด้วย **node fetch** ไม่ใช่ curl (curl บน Windows ส่งภาษาไทยใน body เพี้ยน = mojibake)
- cookie-session ส่ง 2 cookie (`sess` + `sess.sig`) เทสต์ต้องเก็บทั้งคู่

---

## 11. โครงสร้างไฟล์

```
ระบบInventory_Irish/
├── server.js          # Express + REST API + สิทธิ์ + ลอจิกสต็อก/หุ่น/คำขอ
├── db.js              # เชื่อม libsql/Turso + schema + migration + helper (tx/recalc)
├── render.yaml        # config deploy บน Render
├── CLAUDE.md          # คู่มือสั้นเชิงเทคนิค (สำหรับ Claude/นักพัฒนา)
├── DOCS.md            # เอกสารนี้
├── data/inventory.db  # ฐานข้อมูล (โลคัล/replica)
├── public/            # UI ที่ build แล้ว (Express เสิร์ฟจากนี่) — มาจาก client build
└── client/            # ซอร์ส React
    └── src/
        ├── App.jsx          # auth + แท็บ + badge + ตัวตน guest
        ├── Items.jsx        # หน้ารายการของ, Stock Check (UnitsPanel), MoveForm, RequestForm
        ├── Requests.jsx     # หน้าคำขอ (workflow) การ์ด + เลือกหน่วย
        ├── Dashboard.jsx    # แดชบอร์ดสรุป
        ├── Log.jsx          # ประวัติ (admin)
        ├── Users.jsx        # จัดการผู้ใช้ (admin)
        ├── Cart.jsx         # ตะกร้ายืม
        ├── components.jsx   # Modal / Table / Toast / DialogProvider
        ├── api.js           # ตัวเรียก API + CATEGORY_GROUPS (แหล่งความจริงหมวด) + สถานะ
        └── ErrorBoundary.jsx
```

---

## 12. API หลัก (สรุป)

| Method + Path | สิทธิ์ | ทำอะไร |
|---------------|-------|--------|
| `POST /api/login` `/logout` | — | เข้า/ออกระบบ |
| `GET /api/items` | auth | รายการของ (คำนวณ 5 ช่องสต็อกให้แล้ว) |
| `POST /api/items` · `PUT /api/items/:id` | admin | เพิ่ม/แก้ของ (รวมประกอบหุ่น) |
| `DELETE /api/items/:id` | admin | ลบของ (กันถ้ายังมีของถูกยืม/จองในหุ่น) |
| `POST /api/items/:id/move` | admin | เคลื่อนไหวสต็อกของไม่ track (ยืม/คืน/เบิก/รับเข้า/ปรับยอด) + mirror คำขอ |
| `GET/POST /api/items/:id/units` | auth/admin | ดู/สร้างหน่วยย่อย |
| `POST /api/units/:id/move` | admin | เปลี่ยนสถานะหน่วย (ยืม/คืน/พัง/หาย) + mirror คำขอ |
| `POST /api/units/:id/release` | admin | **ปลดหน่วยออกจากหุ่นกลับคลัง** |
| `DELETE /api/units/:id` | admin | เลิกใช้หน่วย (กันถ้า reserved/borrowed) |
| `POST /api/orders` | auth | ส่งคำขอยืม/เบิก (ตะกร้า) |
| `GET /api/requests` | auth | รายการคำขอ (กรองตามสิทธิ์) |
| `POST /api/requests/:id/approve` | admin | อนุมัติ = ตัดสต็อก + เลือกหน่วย |
| `POST /api/requests/:id/return` | admin | รับคืน (ปกติ/พัง/หาย) |
| `POST /api/requests/:id/reject` `/cancel` | admin/เจ้าของ | ปฏิเสธ/ยกเลิก |
| `GET /api/broken` | auth | ของพัง/หาย |
| `GET/DELETE /api/borrowers` | admin | รายชื่อผู้ยืม / ลบผู้ยืม (คืนของก่อน) |
| `GET /api/transactions` | admin | ประวัติทั้งหมด |
| `POST /api/guest/name` | — | guest ยืนยันตัวจากรหัสบัตร (มี throttle) |

---

## 13. สิ่งที่ยังไม่ได้ทำ / ค้างไว้

- Barcode/QR สแกนหน่วย
- แจ้งเตือนอีเมล / เตือนเกินกำหนดคืน
- import ของเดิมจาก CSV
- สคริปต์ backup อัตโนมัติ
- **รวมล็อกอินกับเว็บหลัก (Firebase Auth)** — วางแผนไว้แล้ว: ให้ล็อกอินผ่านเว็บหลักแล้วส่ง ID token มาให้ คลังตรวจด้วย firebase-admin (รอ service account + เลือกที่มาของ role)
- **หนี้เชิงดูแล:** หมวดหมู่มีซ้ำ 2 ที่ (client `api.js` + server `CATEGORY_TYPE`) ควรรวมเป็นที่เดียว
- มีข้อมูลทดสอบปนอยู่ (ชื่อบางอันเพี้ยนจาก curl เก่า) — รอถามผู้ใช้ว่าจะล้างไหม

---

## 14. ประวัติฟีเจอร์สำคัญ (ล่าสุด)

- โมเดลหุ่นแบบ "กันไว้ ไม่ตัดออกจากคลัง" (reserve-on-create) + เลือกหน่วยเจาะจงผูกเข้าหุ่น
- ระบบรหัสบัตรยืนยันตัวตน guest (กันสวมสิทธิ์)
- admin ยืมที่หน้ารายการ → เด้งเป็นคำขอ "ถูกยืมอยู่" อัตโนมัติ (mirror, sync สองทาง)
- ปุ่ม "↩ คืนคลัง" ปลดหน่วยที่อยู่ในหุ่นกลับเป็นของว่าง
- แจ้งพัง/แจ้งหาย ตอนรับคืน · หมวดแบตเตอรี่ + หน่วยความจำ/การ์ด
- แดชบอร์ดแยกการ์ดอุปกรณ์/หุ่น + เลย์เอาต์มือถือ 2 คอลัมน์

---

*อัปเดตล่าสุด: 2026-09-19*
